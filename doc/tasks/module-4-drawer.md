# Module 4: 程式化動態繪圖 (Procedural Drawing)

- [ ] 在 Player 場景中加入 `Node2D` 並掛載腳本 `procedural_drawer.gd`。
- [ ] 實作 `_process()` 函數，每幀讀取所有 `Bone2D` 的絕對座標。
- [ ] 實作 `_draw()` 函數，利用 `draw_line()` 畫出骨骼連線（粗線條像素風），並利用 `draw_circle()` 畫出實心頭部。
